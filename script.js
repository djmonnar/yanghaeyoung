const candidateData = {
  name: "양해영",
  party: "국민의힘",
  ballot: "기호 2",
  role: "경상남도의원 후보",
  displayName: "양해영 경상남도의원 후보",
  election: "경상남도의회의원선거 진주시 제2선거구",
  slogan: "시민의 강력한 힘이 되겠습니다",
  subSlogan: "말보다 행동으로 보여드리겠습니다",
  districts: ["평거동", "신안동", "이현동", "판문동", "명석면", "대평면", "수곡면"],
  heroPoints: [
    "진주시의회 최초 여성의장",
    "3선 도의원 (경남도의회 제9대ㆍ10대의원)",
    "현) 제9대 진주시의회의원",
  ],
  messageTitle: "쉽지 않았지만, 멈추지 않았습니다",
  message: [
    "존경하는 진주시민 여러분, 양해영입니다.",
    "2006년 첫 시의원으로 시작한 길, 오직 시민만 보고 걸어왔습니다.",
    "여성이라는 이유로 더 증명해야 했지만 끝까지 걸어왔습니다.",
    "그 길 위에서 진주시 최초 지역구 여성 시의원, 진주시 최초 지역구 선출 여성 도의원, 진주시의회 최초 여성 의장이 되었습니다.",
    "강력한 추진력으로 진주시민의 약속을 반드시 지키는 양해영이 진주의 자부심을 지키겠습니다.",
  ],
  careers: [
    "진주시의회 최초 여성의장",
    "3선 도의원 (경남도의회 제9대ㆍ10대의원)",
    "현) 제9대 진주시의회의원",
    "전) 제5대 진주시의회의원 후반기 운영위원장",
    "전) 제9대·10대 경상남도의회의원",
    "전) 제9대 진주시의회 전반기 의장",
    "전) 창원지방법원 진주지원 조정의원",
    "전) 경상남도 공익사업선정위원회 위원",
    "전) 경상남도 지역고용심의회 위원",
    "전) 진주시 교육경비보조심의위원회 위원",
    "현) 진주시 노인복지기금운용심의위원회 위원",
    "현) 진주시 지속가능발전협의회 위원",
    "현) 진주시 농산물도매시장 관리운영위원회 위원",
    "현) 진주시 무장애도시추진위원회 위원",
    "현) 진주시 감염병관리위원회 위원",
  ],
  education: [
    "단계국민학교 졸업",
    "신등중학교 졸업",
    "선명여자상업고등학교 졸업",
    "진주전문대학 유아교육과 졸업",
    "진주산업대학교 임산공학과 졸업",
    "진주산업대학교 벤처창업대학원 졸업(아동복지학석사)",
  ],
  records: {
    title: "의장 양해영, 일하는 의회로 증명했습니다",
    cards: [
      {
        title: "지방의회 역할 확립",
        points: [
          "변화된 지방의회제도, 정책지원관, 인사청문회 제도를 안정적으로 안착",
          "윤리특별위원회 상설 운영",
        ],
      },
      {
        title: "입법 전문성 제고",
        points: [
          "의원연구활동 지원 확대",
          "전문성을 갖춘 입법 중심 의회 추진",
          "의원 조례 발의 8대 전반기 19건에서 9대 전반기 77건으로 증가",
        ],
      },
      {
        title: "청렴 의회 실현",
        points: ["진주시의회 종합청렴도 평가 3등급에서 2등급으로 상향 달성"],
      },
    ],
    motions: [
      "진주시의회 토론회 등의 운영에 관한 조례안",
      "진주시 문화콘텐츠산업 진흥 조례안",
      "진주시 생태관광 활성화 및 지원 조례안",
      "진주시 산불방지 및 지원에 관한 조례안",
      "진주시 암환자 가발구입비 지원에 관한 조례안",
      "지속가능한 예산편성 촉구",
      "온실가스감축인지 예산제도 도입 촉구",
      "진주시 생태관광 활성화 촉구",
      "진주형 일하는 밥퍼 사업 추진 제안",
      "진주형 콘텐츠 산업 육성 제안",
    ],
  },
  policies: [
    {
      title: "청년·경제·일자리",
      slogan: "다시 뛰는 진주 경제, 일자리에서 시작합니다",
      image: "images/policy-economy.jpg?v=20260525-policy2",
      points: [
        "청년 일자리 확대 및 정착 지원 확대",
        "청년 주거문화 지원 및 창업 지원 확대",
        "지역산업 맞춤형 일자리 창출",
        "소상공인 지원 강화",
      ],
    },
    {
      title: "돌봄 복지",
      slogan: "삶을 더 따뜻하게, 미래를 더 단단하게",
      image: "images/policy-care.jpg?v=20260525-policy2",
      points: [
        "24시간 대응 가능한 긴급 돌봄 체계 구축",
        "기존 복지시설 활용한 거점형 돌봄 운영",
        "보건·복지·의료 연계 통합 대응 체계 마련",
        "찾아가는 방문형 복지 서비스 확대",
        "이동 지원 및 재가 돌봄 강화",
        "생활권 중심 통합 서비스 구축",
      ],
    },
    {
      title: "교통·안전",
      slogan: "안전은 당연하고, 이동은 편리하게",
      image: "images/policy-safety.jpg?v=20260525-policy2",
      points: [
        "교통체계 개선, 버스 노선 효율화",
        "보행자 안전 강화",
        "어르신·어린이 보호구역 확대",
        "생활안전 인프라 확충, CCTV·조명 등",
      ],
    },
    {
      title: "문화·관광",
      slogan: "진주의 가치를, 더 크게 빛나게",
      image: "images/policy-culture.jpg?v=20260525-policy2",
      points: [
        "유등축제 콘텐츠 활성화",
        "문화관광 인프라 확대",
        "지역 문화예술 지원 확대",
        "체류형 관광도시 기반 조성",
      ],
    },
    {
      title: "노인·장애인 복지",
      slogan: "약속을 넘어 일상이 되는 촘촘한 복지 안전망",
      image: "images/policy-welfare.jpg?v=20260525-policy2",
      points: [
        "이동 지원 서비스 확대",
        "생활·의료 이동 연계 지원 강화",
        "발달장애 특화 복지관 건립 추진",
        "서부 경남 거점형 복지 인프라 구축",
      ],
    },
    {
      title: "여성·가족·교육",
      slogan: "오늘의 안심, 내일의 안정",
      image: "images/policy-education.jpg?v=20260525-policy2",
      points: [
        "경력 보유 여성 재취업 지원 및 직업 훈련 확대",
        "돌봄 서비스 연계 지원",
        "교육환경 개선",
        "이주 여성 상담소 및 보호 체계 구축",
        "다국어 상담 및 긴급 지원 시스템 운영",
        "유보통합 지원 확대",
        "어린이집 취사부 인건비 지원 확대",
      ],
    },
  ],
  neighborhoods: [
    {
      name: "평거동",
      points: [
        "주차난 해소 및 공영주차장 확충",
        "생활편의시설, 상권 활성화 지원",
        "어린이·청소년 문화공간 조성",
        "권역별 청소년 문화의 집 설치",
        "문화·진로·상담 기능 통합 운영",
      ],
    },
    {
      name: "신안동",
      points: [
        "강변 문화거리 조성을 통한 남강문화관광 활성",
        "신안동 주민센터 청사 현대화, 주민 편의시설 확충",
        "골목길 조명·CCTV 설치로 야간 안심길 조성",
        "종합사회복지관 기능 강화",
        "고독사 예방 시스템 구축",
      ],
    },
    {
      name: "이현동",
      points: [
        "석갑산 힐링 건강 공원 조성",
        "도시가스 공급 확대 추진",
        "버스 노선 효율화",
        "종합사회복지관 기능 강화",
        "고독사 예방 시스템 구축",
      ],
    },
    {
      name: "판문동",
      points: [
        "교육환경 개선 및 학습 인프라 지원",
        "수변생활권, 재해안전 개선",
        "진양호반 관광 인프라 구축",
        "권역별 청소년 문화의 집 설치",
        "문화·진로·상담 기능 통합 운영",
      ],
    },
    {
      name: "명석면",
      points: [
        "농지·농로 기반 시설 정비",
        "스마트 농업 지원 확대",
        "서부권 생활체육 인프라 확대",
        "돌봄 농업 + 재가 돌봄 연계",
        "보건소·복지기관 협력 모델 구축",
      ],
    },
    {
      name: "대평면",
      points: [
        "농촌체험·호반 자원 활용한 관광사업",
        "농촌시설·하우스 지원사업 확대",
        "농촌 기반 시설 정비",
        "돌봄 농업 + 재가 돌봄 연계",
        "보건소·복지기관 협력 모델 구축",
      ],
    },
    {
      name: "수곡면",
      points: [
        "딸기 연구소, 다목적문화센터 건립",
        "진주딸기축제 활성화",
        "농촌 시설하우스 지원사업 확대",
        "돌봄 농업 + 재가 돌봄 연계",
        "보건소·복지기관 협력 모델 구축",
      ],
    },
  ],
  contact: {
    publisher: "양해영 선거사무소",
    address: "진주시 진양호로 348(2층)",
    phone: "010-6493-8992",
    agency: "짓마케팅 서효승",
  },
  images: {
    hero: {
      path: "images/hero-yang.jpg",
      alt: "거리에서 인사하는 양해영 경상남도의원 후보",
    },
    profile: {
      path: "images/yang-profile.jpg",
      alt: "양해영 경상남도의원 후보 프로필 사진",
    },
    meeting: {
      path: "images/yang-meeting.jpg",
      alt: "회의 중 발언하는 양해영 후보",
    },
    speech: {
      path: "images/yang-speech.jpg",
      alt: "본회의장에서 연설하는 양해영 후보",
    },
    banner: {
      path: "images/yang-banner.jpg",
      alt: "양해영 후보 선거 홍보 배너",
    },
  },
};

const KAKAO_JAVASCRIPT_KEY = "";
const shareData = {
  url: "https://djmonnar.github.io/yanghaeyoung/",
  title: "양해영 경상남도의원 후보",
  description: "시민의 강력한 힘이 되겠습니다",
  imageUrl: "https://djmonnar.github.io/yanghaeyoung/images/kakao-thumbnail.jpg",
};

const nav = [
  ["홈", "home"],
  ["후보", "profile"],
  ["성과", "record"],
  ["공약", "policies"],
  ["지역", "neighborhoods"],
  ["현장", "gallery"],
  ["연락", "contact"],
];

const $ = (selector) => document.querySelector(selector);
const list = (items) => `<ul class="list">${items.map((item) => `<li>${item}</li>`).join("")}</ul>`;
const image = (item, extra = "") => `<img src="${item.path}" alt="${item.alt}" ${extra}>`;
const districts = candidateData.districts.join(" · ");

function initMenus() {
  const toLink = ([label, view]) => `<a href="#${view}" data-view-link="${view}">${label}</a>`;
  $("#top-menu").innerHTML = nav.map(toLink).join("");
  $(".floating-menu").innerHTML = nav
    .filter(([, view]) => ["home", "policies", "neighborhoods", "gallery"].includes(view))
    .map(toLink)
    .join("");

  const button = $(".menu-toggle");
  const menu = $("#top-menu");
  button.addEventListener("click", () => {
    const open = menu.classList.toggle("is-open");
    button.setAttribute("aria-expanded", open);
  });
  menu.addEventListener("click", () => {
    menu.classList.remove("is-open");
    button.setAttribute("aria-expanded", "false");
  });
}

function hero() {
  return `
    <section class="hero">
      <div class="hero-band">${candidateData.party} · ${candidateData.election}</div>
      <div class="shell hero-layout">
        <div class="hero-copy">
          <div class="hero-badges">
            <span class="ballot-chip">${candidateData.ballot}</span>
            <span>${candidateData.party}</span>
          </div>
          <p class="hero-kicker">${candidateData.displayName}</p>
          <h1>${candidateData.name}</h1>
          <p class="hero-slogan">${candidateData.slogan}</p>
          <div class="hero-proof">
            ${candidateData.heroPoints.map((point) => `<span>${point}</span>`).join("")}
          </div>
          <p class="hero-area">${districts}</p>
          <div class="share-actions">
            <button class="kakao-share" type="button" data-share-kakao aria-label="카카오톡으로 양해영 후보 홈페이지 공유">
              <span>Talk</span>
              카카오톡 공유
            </button>
          </div>
        </div>
        <figure class="hero-media">
          ${image(candidateData.images.hero, 'loading="eager"')}
          <figcaption>
            <b>${candidateData.ballot.replace("기호 ", "")}</b>
            <span>${candidateData.subSlogan}</span>
          </figcaption>
        </figure>
      </div>
    </section>
  `;
}

function home() {
  return `
    <div class="view">
      ${hero()}
      <section class="shell home-brief" data-reveal>
        <div>
          <p class="eyebrow">Candidate</p>
          <h2>${candidateData.messageTitle}</h2>
          <p>${candidateData.message[1]}</p>
          <p>${candidateData.message[4]}</p>
        </div>
        <div class="brief-photo">
          ${image(candidateData.images.profile, 'loading="lazy"')}
        </div>
      </section>
      <section class="shell home-policies">
        <div class="section-head compact">
          <p class="eyebrow">Core Promises</p>
          <h2 class="title">핵심 공약을 한눈에</h2>
          <p class="desc">자세한 내용은 하단 플로팅 메뉴의 공약에서 확인할 수 있습니다.</p>
        </div>
        <div class="policy-preview">
          ${candidateData.policies.slice(0, 3).map(policyPreview).join("")}
        </div>
      </section>
    </div>
  `;
}

function policyPreview(policy) {
  return `
    <article class="policy-preview-card" data-reveal>
      <img src="${policy.image}" alt="${policy.title} 공약 이미지" loading="lazy">
      <div>
        <h3>${policy.title}</h3>
        <p>${policy.slogan}</p>
      </div>
    </article>
  `;
}

function profile() {
  return `
    <div class="view sub-page">
      <div class="shell section-head">
        <p class="eyebrow">Candidate</p>
        <h1 class="title">${candidateData.displayName}</h1>
        <p class="desc">${candidateData.election}</p>
      </div>
      <section class="shell profile-layout">
        <article class="message" data-reveal>
          <h2>${candidateData.messageTitle}</h2>
          ${candidateData.message.map((line) => `<p>${line}</p>`).join("")}
        </article>
        <div class="profile-stack">
          <article class="card" data-reveal>
            <div class="card-head">핵심 이력</div>
            <div class="card-body">${list(candidateData.careers)}</div>
          </article>
          <article class="card" data-reveal>
            <div class="card-head">학력</div>
            <div class="card-body">${list(candidateData.education)}</div>
          </article>
        </div>
      </section>
    </div>
  `;
}

function record() {
  const { records } = candidateData;
  return `
    <div class="view sub-page">
      <div class="shell section-head">
        <p class="eyebrow">Record</p>
        <h1 class="title">${records.title}</h1>
      </div>
      <section class="shell record-grid">
        ${records.cards
          .map(
            (card) => `
              <article class="card record-card" data-reveal>
                <div class="card-head">${card.title}</div>
                <div class="card-body">${list(card.points)}</div>
              </article>
            `
          )
          .join("")}
      </section>
      <section class="shell motion-box" data-reveal>
        <p class="eyebrow">Ordinance & Remarks</p>
        <h2>대표 조례와 발언</h2>
        ${list(records.motions)}
      </section>
    </div>
  `;
}

function policies() {
  return `
    <div class="view sub-page">
      <div class="shell section-head">
        <p class="eyebrow">Promises</p>
        <h1 class="title">6대 핵심 공약</h1>
        <p class="desc">${candidateData.subSlogan}</p>
      </div>
      <section class="shell policy-grid">
        ${candidateData.policies.map(policyCard).join("")}
      </section>
    </div>
  `;
}

function policyCard(policy, index) {
  return `
    <article class="policy-card" data-reveal>
      <div class="policy-image">
        <img src="${policy.image}" alt="${policy.title} 공약 이미지" loading="lazy">
        <span>${String(index + 1).padStart(2, "0")}</span>
      </div>
      <div class="policy-content">
        <h2>${policy.title}</h2>
        <p>${policy.slogan}</p>
        ${list(policy.points)}
      </div>
    </article>
  `;
}

function neighborhoods() {
  return `
    <div class="view sub-page">
      <div class="shell section-head">
        <p class="eyebrow">Neighborhood</p>
        <h1 class="title">살기 좋은 동네는 작은 변화에서 시작됩니다</h1>
        <p class="desc">${districts}</p>
      </div>
      <section class="shell neighborhood-layout">
        <div class="tabs" role="tablist">
          ${candidateData.neighborhoods
            .map((item, index) => `<button class="tab ${index === 0 ? "is-active" : ""}" type="button" data-n="${index}">${item.name}</button>`)
            .join("")}
        </div>
        <article id="neighborhood-card" class="card" data-reveal></article>
      </section>
    </div>
  `;
}

function gallery() {
  const items = [
    ["프로필", candidateData.images.profile],
    ["거리 인사", candidateData.images.hero],
    ["회의·간담회", candidateData.images.meeting],
    ["본회의장 연설", candidateData.images.speech],
    ["선거 홍보 배너", candidateData.images.banner],
    ["현장 활동", { path: "images/gallery-01.jpg", alt: "현장에서 시민과 인사하는 양해영 후보" }],
    ["지역 소통", { path: "images/gallery-02.jpg", alt: "지역 주민과 소통하는 양해영 후보" }],
    ["행사 현장", { path: "images/gallery-03.jpg", alt: "지역 행사 현장의 양해영 후보" }],
    ["봉사 현장", { path: "images/gallery-04.jpg", alt: "현장 활동에 참여한 양해영 후보" }],
  ];

  return `
    <div class="view sub-page">
      <div class="shell section-head">
        <p class="eyebrow">Gallery</p>
        <h1 class="title">현장활동</h1>
        <p class="desc">제공된 사진과 웹용 이미지 자산을 사용했습니다.</p>
      </div>
      <section class="shell gallery">
        ${items
          .map(
            ([title, item]) => `
              <figure data-reveal>
                ${image(item, 'loading="lazy"')}
                <figcaption>${title}</figcaption>
              </figure>
            `
          )
          .join("")}
      </section>
    </div>
  `;
}

function contact() {
  return `
    <div class="view sub-page">
      <section class="shell cta" data-reveal>
        <p class="eyebrow">${candidateData.party} · ${candidateData.ballot}</p>
        <h1>${candidateData.slogan}</h1>
        <p>${candidateData.subSlogan}</p>
        <div class="contact-lines">
          <span>발행처: ${candidateData.contact.publisher}</span>
          <span>주소: ${candidateData.contact.address}</span>
          <span>전화: <a href="tel:${candidateData.contact.phone.replaceAll("-", "")}">${candidateData.contact.phone}</a></span>
          <span>제작: ${candidateData.contact.agency}</span>
        </div>
        <button class="kakao-share cta-share" type="button" data-share-kakao aria-label="카카오톡으로 양해영 후보 홈페이지 공유">
          <span>Talk</span>
          카카오톡 공유
        </button>
      </section>
    </div>
  `;
}

const views = { home, profile, record, policies, neighborhoods, gallery, contact };

function render(view = (location.hash || "#home").slice(1)) {
  if (!views[view]) view = "home";
  $("#app").innerHTML = views[view]();
  document.querySelectorAll("[data-view-link]").forEach((link) => {
    link.classList.toggle("is-active", link.dataset.viewLink === view);
  });
  if (view === "neighborhoods") bindNeighborhoods();
  bindShareButtons();
  renderFooter();
  reveal();
  window.scrollTo({ top: 0, behavior: "auto" });
}

function bindShareButtons() {
  document.querySelectorAll("[data-share-kakao]").forEach((button) => {
    button.addEventListener("click", shareToKakao);
  });
}

async function shareToKakao() {
  if (KAKAO_JAVASCRIPT_KEY && window.Kakao?.Share) {
    if (!window.Kakao.isInitialized()) {
      window.Kakao.init(KAKAO_JAVASCRIPT_KEY);
    }
    window.Kakao.Share.sendDefault({
      objectType: "feed",
      content: {
        title: shareData.title,
        description: shareData.description,
        imageUrl: shareData.imageUrl,
        link: {
          mobileWebUrl: shareData.url,
          webUrl: shareData.url,
        },
      },
      buttons: [
        {
          title: "홈페이지 보기",
          link: {
            mobileWebUrl: shareData.url,
            webUrl: shareData.url,
          },
        },
      ],
    });
    return;
  }

  if (navigator.share) {
    try {
      await navigator.share({
        title: shareData.title,
        text: shareData.description,
        url: shareData.url,
      });
      return;
    } catch (error) {
      if (error.name === "AbortError") return;
    }
  }

  await navigator.clipboard?.writeText(shareData.url);
  showToast("공유 링크를 복사했습니다");
}

function showToast(message) {
  const oldToast = document.querySelector(".share-toast");
  if (oldToast) oldToast.remove();
  const toast = document.createElement("div");
  toast.className = "share-toast";
  toast.textContent = message;
  document.body.append(toast);
  requestAnimationFrame(() => toast.classList.add("is-visible"));
  setTimeout(() => toast.remove(), 2200);
}

function bindNeighborhoods() {
  const card = $("#neighborhood-card");
  const show = (index) => {
    const item = candidateData.neighborhoods[index];
    card.innerHTML = `<div class="card-head">${item.name}</div><div class="card-body">${list(item.points)}</div>`;
  };
  show(0);
  document.querySelectorAll(".tab").forEach((button) => {
    button.addEventListener("click", () => {
      document.querySelectorAll(".tab").forEach((tab) => tab.classList.remove("is-active"));
      button.classList.add("is-active");
      show(Number(button.dataset.n));
    });
  });
}

function renderFooter() {
  $(".site-footer").innerHTML = `
    <div class="shell">
      <strong>${candidateData.party} · ${candidateData.ballot} · ${candidateData.displayName}</strong>
      <p>${candidateData.election}<br>${districts}</p>
      <p>발행처: ${candidateData.contact.publisher}<br>주소: ${candidateData.contact.address}<br>전화: ${candidateData.contact.phone}<br>제작: ${candidateData.contact.agency}</p>
    </div>
  `;
}

function reveal() {
  const items = document.querySelectorAll("[data-reveal]");
  if (!("IntersectionObserver" in window)) {
    items.forEach((item) => item.classList.add("is-visible"));
    return;
  }
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.12 }
  );
  items.forEach((item) => observer.observe(item));
}

window.addEventListener("hashchange", () => render());
initMenus();
render();
